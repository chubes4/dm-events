/**
 * Event Details Block Frontend
 *
 * Import CSS here so webpack can extract it into build/frontend.css
 * WordPress will automatically enqueue the extracted CSS via block.json
 *
 * @package DataMachineEvents
 * @since 1.0.0
 */

import './frontend.css';
